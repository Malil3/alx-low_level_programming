#include "main.h"
/**
 * add - adds two integers and returns the result
 * @sawi: int to be added to didi
 * @didi: int to be added to sawi
 * Return: sum of sawi and didi
 */

int add(int sawi, int didi)
{
	return (sawi + didi);
}

#include <stdio.h>
/**
 * main - entry point
 *
 * Return:0
 */

int main(void)
{
	char p;

	int q;

	p = 'a';
	q = 0;
	while
		(q < 10) {
			putchar(q + '0');
			q++;
		}
	while
		(p <= 'f') {
			putchar(p);
			p++;
		}
	putchar('\n');
	return (0);
}


